{
  "name": "scraping-guards-test-page",
  "version": "1.0.0",
  "private": true,
  "description": "A test page implementing many anti-scraping guards, with a Playwright suite for CI.",
  "scripts": {
    "serve": "node server.js",
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium",
    "font": "python3 tools/generate-cipher-font.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
